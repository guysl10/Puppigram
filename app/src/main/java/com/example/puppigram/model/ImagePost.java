package com.example.puppigram.model;

/**
 * Represent a image kind of post.
 */
public class ImagePost extends AbstractPost{
    public ImagePost(int id, int owner_id, String title) {
        super(id, owner_id, title);
    }
}
